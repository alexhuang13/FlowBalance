# SDPO (Self-Distillation Policy Optimization) recipe for the unified FlowSD stack.
#
# This package reproduces the SDPO training + GRPO baseline + AIME24 evaluation
# capabilities on top of stable_rl's clean verl submodule, using a pure-addition
# overlay (subclass) approach. No existing file under `core/` or `verl/` is modified.
